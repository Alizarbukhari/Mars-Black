/** @type {import('next').NextConfig} */
const nextConfig = {
  // Copy your existing config options here, but remove TypeScript syntax
  // For example:
  reactStrictMode: true,
  // other options...
}

module.exports = nextConfig 